echo "Running type checks..."
# Backend Type Check
echo "Checking Backend..."
npm exec --prefix backend -- tsc --noEmit

# Frontend Type Check
echo "Checking Frontend..."
npm exec --prefix frontend -- tsc --noEmit
